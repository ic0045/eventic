import { Card, CardContent, CardHeader } from "@mui/material";

export const Dashboard = () => (
  <Card>
    <CardHeader title="Seja bem vindo ao portal Administrativo" />
    <CardContent>Navegue pelo menu ao lado</CardContent>
  </Card>
);